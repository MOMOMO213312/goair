> [!IMPORTANT]
> Read `README.md` first — it documents the platform's actual current state
> (portals, booking rules, known gaps). Avoid rewriting published git history
> (force pushing, rebasing/amending/squashing already-pushed commits) and keep
> the `main` branch in a working state at all times.
